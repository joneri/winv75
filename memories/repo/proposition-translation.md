- For EX proposition rules, avoid broad runtime normalization of spaced `100 000 kr` amounts; it can remap older exact concrete sentences onto placeholder templates that have no matching rule.
- Prefer narrow sentence-family normalization fixes in both `backend/src/proposition/proposition-translation-service.js` and `scripts/audit-proposition-translations.js` when audit and runtime diverge on a specific EX template.
- When promoting EX exact rules for sentences that already have established hardcoded/runtime wording, align the new `fi`/`en` renders to the existing runtime phrasing; otherwise tests may still fail even though the sentence now rule-matches.
- Many remaining EX admin misses are tiny wording variants of already-covered concepts (`tas ut` vs `uttages`, `avgör lottning` vs `gäller lottning`, typoed `giltlig`); prefer explicit exact-rule promotion over new normalization when fallback output is the only problem.
- When an EX amount row moves from fallback/partial coverage to an explicit amount-variable rule, existing English output may preserve source formatting like `10.000` instead of normalizing to `10,000`; update stale expectations instead of forcing new formatting behavior unless the contract changes.
- Qualifier-flow EX rows often come in near-duplicate wording variants (`vid anmälan`, `vid startanmälan`, short tie-break sentences, explicit counts per qualifier); bundle those exact variants together and reuse the nearest accepted phrasing instead of inventing a new generalized matcher.
- EX final-admin rows also cluster well by event ownership: invitations, host-track seat selection, final-start obligations, and short start-ban notices around the same final are usually safer to promote together as one exact bundle than to chase isolated prize fragments.
- Some remaining EX misses are sentence fragments created upstream by punctuation splitting; if those fragment rows recur in audit and fallback output is obviously broken, it is still acceptable to promote the stable fragment text directly as an exact rule instead of widening normalization.
- Short EX admin rows also cluster by operational tone: driver lists, A-licence restrictions, `OBS!` warnings, split-race notices, and proposition-priority instructions can be bundled together safely even when they are not tied to the same named series.
- When an EX sentence normalizes repeated values onto one placeholder key and the audited corpus still only shows one stable concrete variant, it is acceptable to match the normalized template and use a fixed exact render for that proven sentence instead of broadening normalization immediately.
- Once the remaining non-generic EX backlog gets thin, stable repeated fragments with markup residue or truncated numbering can still be worth exact-rule cleanup if they recur across multiple races and otherwise stay fully Swedish or malformed in fallback output.
- After the larger families are exhausted, the best remaining EX slices often combine short qualification-progression notices with one or two nearby series or driver-list one-liners; that mix is still safe if all rows are stable, repeated, and visibly broken in fallback output.
- Late in the EX backlog, short final-property notices like `Finalen är ett varmblodslopp.` or `Finaldistans ... spårtrappa.` can be bundled with nearby qualification follow-up lines instead of waiting for a larger thematic family, as long as all rows are recurring and exact enough to stay rule-only.
- Very late in the EX backlog, the best remaining slices are often just five short recurring exact notices from adjacent race flows; that is still a good increment if it removes visibly broken fallback output and avoids the broad generic families.
- When the EX backlog gets very thin, a coherent late-stage slice can still mix a stable fragment, a short prize row, a final-eligibility notice, and paired distance lines if those rows recur together in audit output and the fallback result is visibly mixed-language or untranslated.
- The current-year variants of already accepted rider-distance rows are good late-stage exact promotions; matching the proven earlier-year phrasing is safer than widening normalization for that family.
- Once the clean exact backlog becomes mostly short dated notices, one viable late-stage bundle is a mix of calendar rows, draw instructions, and dated final-obligation variants, while the more malformed merged rows are better deferred to a later fragment-focused slice.
- Late in the EX backlog, clean score-scale rows can still bundle well with one nearby series-rule intro or invitation rule; they are safer exact promotions than the malformed joined rows that may sit above them in the unmatched list.
- A new exact row can legitimately upgrade an older mixed sentence from `partial-rule-match` to `rule-match`; when that happens, update the older regression expectation instead of treating it as a regression.
- When typo-driven or merged rows sit near the top of the unmatched EX list, it is still safe to skip them and take the clean cluster underneath, for example one post-allocation rule plus a few Summer Meeting calendars and a short qualification-progression sentence.
- Do not promote `Tillägg {allowance_text}.` into EX just because it is a clean unmatched row; the shared allowance-text render still leaves Swedish allowance segments untouched, so an exact EX rule there would only convert fallback output into a poor rule-match.
- Stable EX promo links and nearby markup-bearing admin clauses can still form one safe late-stage exact bundle when they belong to the same recurring event family, such as Elitloppet/UET promo lines plus bold surveillance-stable notices.
- Late-stage EX cleanup can still bundle short start-point ranking rules, A/B-final capacity notices, fee lines, and repeated dead-heat cutoff variants as one coherent exact slice; even a recurring English-source admin sentence is worth promoting if it still counts as unmatched audit residue.
- Late-stage EX qualification cleanup still bundles well by recurring placement thresholds: a top-15 final-eligibility sentence plus nearby `top five/six/seven qualify to the later final` variants and a couple of track-specific final follow-up rows are a safe exact-only slice even when they come from different tracks.
- When the remaining clean EX residue gets thinner, a smaller four-row slice is still fine if it removes obviously broken fallback output from stable repeated rows such as B-final participation, final-start obligations, Breeders Course qualification notices, and short heat-timing instructions.
- Late-stage EX participant-admin cleanup can still bundle well with nearby final-selection rows: short `drivers/riders with most points`, `participating driver may not enter own horse`, `drivers drive owned horse first`, and Treåringseliten/Breeders Course final-selection lines form a coherent exact slice without needing any generic normalization changes.